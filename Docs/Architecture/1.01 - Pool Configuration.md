# Overview
The core of what enables [[1.00 - Entity Pools|Entity Pool's]] performance gains and enhanced type safety is its ability to be configured on an individual basis.  Each pool having its own separate compile-time configuration means the internal architecture of each pool is generated uniquely, meaning:
- Entity-component data can be stored 